import Image from 'next/image'
import materialManifest from '../.codex-delivery/material-fact-manifest.json'

const mediaNames=['image3.jpeg','image4.jpeg','image5.jpeg','image6.jpeg','image7.jpeg','image8.jpeg','image9.jpeg','image10.jpeg','image11.jpeg']

export function MaterialEvidence(){const facts=materialManifest.facts.filter(f=>f.decision==='use'&&'source_value' in f);return <section className="mt-16 border-t pt-12"><h2 className="text-3xl font-semibold">Documented capabilities & source facts</h2><p className="mt-3 max-w-3xl text-muted-foreground">The following expandable record preserves the customer-provided operational, product and catalogue information without inventing missing details.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{mediaNames.map((name,index)=><figure key={name} className="overflow-hidden rounded-sm border bg-card"><Image src={`/images/evidence/${name}`} alt={`Customer-supplied facility evidence ${index+1}`} width={1000} height={750} className="aspect-[4/3] h-auto w-full object-cover"/><figcaption className="p-3 text-xs text-muted-foreground">Customer-supplied facility evidence {index+1}</figcaption></figure>)}</div><details className="mt-10 rounded-sm border bg-card p-5"><summary className="cursor-pointer text-lg font-semibold">Complete audited source-fact record ({facts.length} entries)</summary><dl className="mt-5 divide-y">{facts.map(f=><div key={f.fact_id} className="py-4"><dt className="text-xs font-semibold uppercase tracking-wide text-primary">{f.entity_key}</dt><dd className="mt-2 whitespace-pre-wrap text-sm leading-6 text-muted-foreground">{String(f.source_value)}</dd></div>)}</dl></details></section>}
